// export function setupCounter(element, initialCount = 0) {
//     let counter = initialCount;
//     const setCounter = (count) => {
//       counter = count;
//       element.innerHTML = `Count: ${counter}`;
//     }
//     setCounter(counter);
//   }
  